---
import Layout from '../layouts/Layout.astro';
import '@phosphor-icons/web/regular';
---

<Layout title="Ch0wn3rs - Elite Cybersecurity Club">
  <!-- Hero Section -->
  <section id="home" class="hero">
    <canvas id="circuitCanvas" class="hero-background"></canvas>
    <div class="container">
      <div class="hero-content">
        <div class="brand-identity">
          <img src="/Logo-qr.jpeg" alt="ch0wn3rs" class="brand-logo">
          <h1 class="brand-name">CH0WN3RS</h1>
        </div>
        <p class="hero-subtitle">Elite Cybersecurity Club</p>
        <p class="hero-description">
          We are the digital guardians, the code breakers, the system architects. 
          Join us in the relentless pursuit of cybersecurity excellence.
        </p>
      </div>
      
      <div class="hero-stats">
        <div class="stats-card">
          <div class="best-badge">
            <i class="ph ph-trophy"></i>
            <span class="badge-text" id="colombiaRank">Loading...</span>
            <span class="flag">🇨🇴</span>
          </div>
          
          <div class="stat-item">
            <i class="ph ph-globe"></i>
            <div class="stat-content">
              <span class="stat-label">Global Rank</span>
              <span class="stat-value" id="globalRank">Loading...</span>
            </div>
          </div>
          
          <div class="stat-item">
            <i class="ph ph-chart-line"></i>
            <div class="stat-content">
              <span class="stat-label">Rating</span>
              <span class="stat-value" id="rating">Loading...</span>
            </div>
          </div>
          
          <div class="stat-item">
            <i class="ph ph-calendar-check"></i>
            <div class="stat-content">
              <span class="stat-label">Active Since</span>
              <span class="stat-value" id="yearPoints">Loading...</span>
            </div>
          </div>
          
          <a href="https://ctftime.org/team/408704" target="_blank" class="ctftime-link">
            View on CTFtime <i class="ph ph-arrow-square-out"></i>
          </a>
          
          <p class="ctftime-attribution">Powered by CTFtime</p>
        </div>
      </div>
    </div>
  </section>

  <script is:inline>
    window.addEventListener('load', function() {
      const canvas = document.getElementById('circuitCanvas');
      if (!canvas) {
        console.error('Canvas not found');
        return;
      }
      
      const ctx = canvas.getContext('2d');

      function resizeCanvas() {
        canvas.width = window.innerWidth;
        canvas.height = window.innerHeight;
        initDrops();
      }
      
      // Matrix configuration
      const fontSize = 14;
      let columns;
      let drops = [];
      
      // Characters for matrix effect
      const matrixChars = 'ｱｲｳｴｵｶｷｸｹｺｻｼｽｾｿﾀﾁﾂﾃﾄﾅﾆﾇﾈﾉﾊﾋﾌﾍﾎﾏﾐﾑﾒﾓﾔﾕﾖﾗﾘﾙﾚﾛﾜｦﾝ0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZ';
      
      function initDrops() {
        columns = Math.floor(canvas.width / fontSize);
        drops = [];
        // Initialize all drops at different heights
        for (let i = 0; i < columns; i++) {
          drops[i] = Math.floor(Math.random() * canvas.height / fontSize);
        }
      }
      
      resizeCanvas();
      window.addEventListener('resize', resizeCanvas);

      // Draw function
      let frameCount = 0;
      function draw() {
        frameCount++;
        
        // Black background with transparency for fade effect
        ctx.fillStyle = 'rgba(0, 0, 0, 0.1)';
        ctx.fillRect(0, 0, canvas.width, canvas.height);

        ctx.font = fontSize + 'px monospace';
        
        // Only update drops every 3 frames for slower animation
        if (frameCount % 3 === 0) {
          for (let i = 0; i < drops.length; i++) {
            // Random character
            const char = matrixChars[Math.floor(Math.random() * matrixChars.length)];
            
            // Position
            const x = i * fontSize;
            const y = drops[i] * fontSize;
            
            // Gradient effect - brighter at the head
            const brightness = Math.random();
            if (brightness > 0.98) {
              ctx.fillStyle = '#ffffff';
            } else if (brightness > 0.9) {
              ctx.fillStyle = '#ff1744';
            } else {
              const opacity = Math.random() * 0.5 + 0.5;
              ctx.fillStyle = `rgba(220, 20, 60, ${opacity})`;
            }
            ctx.fillText(char, x, y);
            
            // Move drop down
            if (y > canvas.height && Math.random() > 0.975) {
              drops[i] = 0;
            }
            drops[i]++;
          }
        }

        requestAnimationFrame(draw);
      }

      // Start animation
      draw();
    });
  </script>

  <script is:inline>
    // CTFtime API data fetch
    window.addEventListener('load', async function() {
      const TEAM_ID = '408704';
      
      try {
        // Fetch team info from our API route
        const response = await fetch(`/api/ctftime?teamId=${TEAM_ID}`);
        const data = await response.json();
        
        if (data.error) {
          throw new Error(data.error);
        }
        
        console.log('CTFtime data:', data);
        
        // Update Colombia rank badge
        document.getElementById('colombiaRank').textContent = data.colombiaRank;
        
        // Update global rank
        document.getElementById('globalRank').textContent = data.globalRank;
        
        // Update rating
        document.getElementById('rating').textContent = data.rating;
        
        // Update active since
        document.getElementById('yearPoints').textContent = data.activeSince;
        
      } catch (error) {
        console.error('Error fetching CTFtime data:', error);
        document.getElementById('colombiaRank').textContent = '#1 COLOMBIA';
        document.getElementById('globalRank').textContent = 'N/A';
        document.getElementById('rating').textContent = 'N/A';
        document.getElementById('yearPoints').textContent = 'N/A';
      }
    });
  </script>

  <script is:inline>
    // Accordion functionality
    document.addEventListener('DOMContentLoaded', function() {
      const accordionHeaders = document.querySelectorAll('.accordion-header');
      
      accordionHeaders.forEach(header => {
        header.addEventListener('click', function() {
          const accordionItem = this.parentElement;
          const isActive = accordionItem.classList.contains('active');
          
          // Close all accordion items
          document.querySelectorAll('.accordion-item').forEach(item => {
            item.classList.remove('active');
          });
          
          // Toggle current item
          if (!isActive) {
            accordionItem.classList.add('active');
          }
        });
      });
      
      // All items closed by default (no auto-open)
    });
  </script>

  <!-- About Section -->
  <section id="about" class="about">
    <div class="container">
      <h2 class="section-title">About Ch0wn3rs</h2>
      
      <div class="about-content">
        <!-- Left Side: Text + Stats -->
        <div class="about-left">
          <p>
            Ch0wn3rs is more than just a cybersecurity club – we're a community of passionate 
            digital defenders dedicated to mastering the art of ethical hacking, pentesting, 
            and cybersecurity research.
          </p>
          <p>
            Our mission is to cultivate the next generation of cybersecurity professionals through 
            hands-on learning, collaborative problem-solving, and real-world challenges that prepare 
            our members for the evolving threat landscape.
          </p>
          
          <div class="stats">
            <div class="stat">
              <div class="stat-number">6</div>
              <div class="stat-label">Active Members</div>
            </div>
            <div class="stat">
              <div class="stat-number">0</div>
              <div class="stat-label">CTF Wins</div>
            </div>
            <div class="stat">
              <div class="stat-number">24/7</div>
              <div class="stat-label">Learning</div>
            </div>
          </div>
        </div>

        <!-- Right Side: Core Competencies -->
        <div class="about-right">
          <h3>Core Competencies</h3>
          <div class="accordion-list">
            <div class="accordion-item">
              <div class="accordion-header">
                <i class="ph ph-shield-check accordion-icon"></i>
                <h4>Pentesting</h4>
                <i class="ph ph-caret-down accordion-arrow"></i>
              </div>
              <div class="accordion-content">
                <p>Ethical hacking and vulnerability assessment</p>
                <ul class="skill-details">
                  <li>Web application penetration testing</li>
                  <li>Network security assessments</li>
                  <li>Mobile application security</li>
                </ul>
              </div>
            </div>

            <div class="accordion-item">
              <div class="accordion-header">
                <i class="ph ph-magnifying-glass accordion-icon"></i>
                <h4>Digital Forensics</h4>
                <i class="ph ph-caret-down accordion-arrow"></i>
              </div>
              <div class="accordion-content">
                <p>Incident response and evidence analysis</p>
                <ul class="skill-details">
                  <li>Memory forensics and analysis</li>
                  <li>Network traffic investigation</li>
                  <li>Malware reverse engineering</li>
                </ul>
              </div>
            </div>

            <div class="accordion-item">
              <div class="accordion-header">
                <i class="ph ph-sword accordion-icon"></i>
                <h4>Red Team Operations</h4>
                <i class="ph ph-caret-down accordion-arrow"></i>
              </div>
              <div class="accordion-content">
                <p>Advanced threat simulation and testing</p>
                <ul class="skill-details">
                  <li>Social engineering campaigns</li>
                  <li>Physical security assessments</li>
                  <li>Advanced persistent threat simulation</li>
                </ul>
              </div>
            </div>

            <div class="accordion-item">
              <div class="accordion-header">
                <i class="ph ph-wrench accordion-icon"></i>
                <h4>Tool Development</h4>
                <i class="ph ph-caret-down accordion-arrow"></i>
              </div>
              <div class="accordion-content">
                <p>Custom security tools and automation</p>
                <ul class="skill-details">
                  <li>Python security scripting</li>
                  <li>Exploit development frameworks</li>
                  <li>Automation and CI/CD security</li>
                </ul>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Services Section -->
  <section id="services" class="services">
    <div class="container">
      <h2 class="section-title">What We Offer</h2>
      <div class="services-grid">
        <div class="service-card">
          <div class="service-icon"><i class="ph ph-target"></i></div>
          <h3>CTF Competitions</h3>
          <p>Regular Capture The Flag events to sharpen your skills and compete with the best minds in cybersecurity.</p>
        </div>
        <div class="service-card">
          <div class="service-icon"><i class="ph ph-books"></i></div>
          <h3>Workshops & Training</h3>
          <p>Hands-on workshops covering everything from basic security concepts to advanced exploitation techniques.</p>
        </div>
        <div class="service-card">
          <div class="service-icon"><i class="ph ph-handshake"></i></div>
          <h3>Mentorship Program</h3>
          <p>Connect with industry professionals and experienced members for guidance and career development.</p>
        </div>
        <div class="service-card">
          <div class="service-icon"><i class="ph ph-trophy"></i></div>
          <h3>Certification Support</h3>
          <p>Study groups and resources for industry certifications like CEH, OSCP, CISSP, and more.</p>
        </div>
        <div class="service-card">
          <div class="service-icon"><i class="ph ph-users"></i></div>
          <h3>Community & Events</h3>
          <p>Join our active community, participate in meetups, conferences, and networking events with fellow security enthusiasts.</p>
        </div>
        <div class="service-card coming-soon">
          <div class="service-icon"><i class="ph ph-flag"></i></div>
          <h3>CTF Platform</h3>
          <p>Access our custom CTF platform with challenges ranging from beginner to advanced levels, available 24/7.</p>
          <div class="coming-soon-badge">Coming Soon</div>
        </div>
      </div>
    </div>
  </section>

  <!-- Team Section -->
  <section id="team" class="team">
    <div class="container">
      <h2 class="section-title">Meet The Team</h2>
      <div class="team-grid">
        <div class="team-member">
          <div class="member-avatar"><i class="ph ph-key"></i></div>
          <h3>RIOS</h3>
          <p class="member-role">CRYPTOGRAPHY</p>
          <p class="member-bio">Focused on solving <strong>cryptography</strong> challenges</p>
          <p class="member-email">rios@ch0wn3rs.ninja</p>
        </div>
        <div class="team-member">
          <div class="member-avatar"><i class="ph ph-brain"></i></div>
          <h3>02loveslollipop</h3>
          <p class="member-role">AI & DATA</p>
          <p class="member-bio">Focused on solving <strong>AI & data</strong> challenges</p>
          <p class="member-email">zerotwo@ch0wn3rs.ninja</p>
        </div>
        <div class="team-member">
          <div class="member-avatar"><i class="ph ph-binoculars"></i></div>
          <h3>hrcamilo</h3>
          <p class="member-role">OSINT</p>
          <p class="member-bio">Focused on solving <strong>OSINT</strong> challenges</p>
          <p class="member-email">hrcamilo@ch0wn3rs.ninja</p>
        </div>
        <div class="team-member">
          <div class="member-avatar"><i class="ph ph-gear"></i></div>
          <h3>InfinityPaiin</h3>
          <p class="member-role">REVERSING</p>
          <p class="member-bio">Focused on solving <strong>reversing</strong> challenges</p>
          <p class="member-email">infinitypaiin@ch0wn3rs.ninja</p>
        </div>
        <div class="team-member">
          <div class="member-avatar"><i class="ph ph-detective"></i></div>
          <h3>Neyi21</h3>
          <p class="member-role">FORENSICS</p>
          <p class="member-bio">Focused on solving <strong>forensics</strong> challenges</p>
          <p class="member-email">neyi21@ch0wn3rs.ninja</p>
        </div>
        <div class="team-member">
          <div class="member-avatar"><i class="ph ph-globe"></i></div>
          <h3>Fu11shoot</h3>
          <p class="member-role">PWN & WEB</p>
          <p class="member-bio">Focused on solving <strong>pwn & web</strong> challenges</p>
          <p class="member-email">fu11shoot@ch0wn3rs.ninja</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Social Media Section -->
  <section id="social" class="social-media">
    <div class="container">
      <h2 class="section-title">Connect With Us</h2>
      <p class="social-subtitle">Join our community and stay updated with the latest news, events, and challenges</p>
      <div class="social-links">
        <a href="https://www.linkedin.com/company/ch0wn3rs/" target="_blank" rel="noopener noreferrer" class="social-card">
          <div class="social-icon">
            <i class="ph ph-linkedin-logo"></i>
          </div>
          <h3>LinkedIn</h3>
          <p>Follow us for professional updates</p>
        </a>
        <a href="https://www.instagram.com/ctf_upb?igsh=MWd5Y3VsNm5zYjVzMw%3D%3D&utm_source=qr" target="_blank" rel="noopener noreferrer" class="social-card">
          <div class="social-icon">
            <i class="ph ph-instagram-logo"></i>
          </div>
          <h3>Instagram</h3>
          <p>See our latest activities and events</p>
        </a>
        <a href="https://discord.gg/7kfEz7xQKp" target="_blank" rel="noopener noreferrer" class="social-card">
          <div class="social-icon">
            <i class="ph ph-discord-logo"></i>
          </div>
          <h3>Discord</h3>
          <p>Join our active community chat</p>
        </a>
        <a href="https://ctftime.org/team/408704" target="_blank" rel="noopener noreferrer" class="social-card">
          <div class="social-icon">
            <i class="ph ph-trophy"></i>
          </div>
          <h3>CTFtime</h3>
          <p>Check our rankings and CTF history</p>
        </a>
      </div>
    </div>
  </section>


  <!-- Footer -->
  <footer class="footer">
    <div class="container">
      <div class="footer-content">
        <div class="footer-brand">
          <h3 class="logo">ch0wn3rs</h3>
          <p>Elite Cybersecurity Club</p>
          <p class="footer-tagline">Mastering the art of ethical hacking since 2020</p>
        </div>
        <div class="footer-links">
          <div class="footer-column">
            <h4>Quick Links</h4>
            <ul>
              <li><a href="#about">About Us</a></li>
              <li><a href="#services">Services</a></li>
              <li><a href="#team">Our Team</a></li>
              <li><a href="#social">Social Media</a></li>
            </ul>
          </div>
          <div class="footer-column">
            <h4>Connect</h4>
            <ul>
              <li><a href="https://discord.gg/7kfEz7xQKp" target="_blank">Discord Community</a></li>
              <li><a href="https://www.linkedin.com/company/ch0wn3rs/" target="_blank">LinkedIn</a></li>
              <li><a href="https://www.instagram.com/ctf_upb?igsh=MWd5Y3VsNm5zYjVzMw%3D%3D&utm_source=qr" target="_blank">Instagram</a></li>
              <li><a href="https://ctftime.org/team/408704" target="_blank">CTFtime Profile</a></li>
            </ul>
          </div>
          <div class="footer-column">
            <h4>Resources</h4>
            <ul>
              <li><a href="https://ch0wn3rs.ninja" target="_blank">Official Website</a></li>
              <li><a href="#services">Workshops & Training</a></li>
              <li><a href="#team">Mentorship Program</a></li>
              <li><a href="#about">Core Competencies</a></li>
            </ul>
          </div>
        </div>
      </div>
      <div class="footer-bottom">
        <p>&copy; 2026 ch0wn3rs. All rights reserved. | <span class="footer-warning"><i class="ph ph-shield-warning"></i> Ethical Hacking Only</span></p>
        <p class="footer-subtitle">Colombia's #1 CTF Team | Cultivating the next generation of cybersecurity professionals</p>
      </div>
    </div>
  </footer>
</Layout>

<style>
  /* Hero Section */
  .hero {
    min-height: 100vh;
    height: 100vh;
    display: flex;
    align-items: center;
    position: relative;
    background: linear-gradient(135deg, #000000 0%, #1a0000 100%);
    overflow: hidden;
    padding: 0 20px;
  }
  
  .hero-background {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: #000000;
  }
  
  .hero .container {
    display: grid;
    grid-template-columns: 1fr 420px;
    gap: 4rem;
    align-items: center;
    position: relative;
    z-index: 1;
    min-height: 100vh;
    max-width: 1500px;
    padding: 0 2rem;
    margin: 0 auto;
  }
  
  .hero-content {
    text-align: center;
    animation: fadeIn 1s ease-out;
  }
  
  .hero-stats {
    animation: fadeInRight 1s ease-out 0.3s both;
  }
  
  .stats-card {
    background: rgba(0, 0, 0, 0.8);
    border: 2px solid var(--primary-red);
    border-radius: 12px;
    padding: 2rem;
    box-shadow: 0 0 40px rgba(220, 20, 60, 0.3);
    backdrop-filter: blur(10px);
  }
  
  .best-badge {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    background: linear-gradient(135deg, var(--primary-red), #ff1744);
    padding: 1rem;
    border-radius: 8px;
    margin-bottom: 1.5rem;
    font-weight: bold;
    font-size: 1.3rem;
    color: white;
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
    animation: pulse 2s ease-in-out infinite;
  }
  
  .best-badge i {
    font-size: 1.8rem;
  }
  
  .flag {
    font-size: 1.5rem;
  }
  
  .stat-item {
    display: flex;
    align-items: center;
    gap: 1rem;
    padding: 1rem;
    border-bottom: 1px solid rgba(220, 20, 60, 0.2);
    transition: all 0.3s ease;
  }
  
  .stat-item:last-of-type {
    border-bottom: none;
    margin-bottom: 1rem;
  }
  
  .stat-item:hover {
    background: rgba(220, 20, 60, 0.1);
    transform: translateX(5px);
  }
  
  .stat-item i {
    font-size: 2rem;
    color: var(--primary-red);
  }
  
  .stat-content {
    display: flex;
    flex-direction: column;
    flex: 1;
  }
  
  .stat-label {
    font-size: 0.85rem;
    color: #888;
    text-transform: uppercase;
    letter-spacing: 1px;
  }
  
  .stat-value {
    font-size: 1.5rem;
    font-weight: bold;
    color: white;
    text-shadow: 0 0 10px rgba(220, 20, 60, 0.5);
  }
  
  .ctftime-link {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    padding: 0.8rem;
    background: transparent;
    border: 1px solid var(--primary-red);
    border-radius: 6px;
    color: var(--primary-red);
    text-decoration: none;
    transition: all 0.3s ease;
    font-size: 0.9rem;
  }
  
  .ctftime-link:hover {
    background: var(--primary-red);
    color: white;
    box-shadow: 0 0 20px rgba(220, 20, 60, 0.5);
    transform: translateY(-2px);
  }
  
  .ctftime-attribution {
    text-align: center;
    font-size: 0.7rem;
    color: rgba(255, 255, 255, 0.4);
    margin-top: 0.8rem;
    font-weight: 300;
  }
  
  @keyframes fadeInRight {
    from {
      opacity: 0;
      transform: translateX(30px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }
  
  .brand-identity {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 2rem;
    margin-bottom: 2rem;
    animation: slideInFromLeft 1s ease-out;
  }
  
  .brand-logo {
    width: 150px;
    height: 150px;
    border-radius: 12px;
    box-shadow: 0 10px 40px rgba(220, 20, 60, 0.4);
    transition: all 0.4s ease;
  }
  
  .brand-logo:hover {
    transform: scale(1.08) rotate(2deg);
    box-shadow: 0 15px 60px rgba(220, 20, 60, 0.6);
  }
  
  .brand-name {
    font-size: 5rem;
    font-weight: 800;
    color: #fff;
    letter-spacing: 3px;
    margin: 0;
    text-shadow:
      0 0 10px #dc143c,
      0 0 20px #dc143c,
      0 0 40px #000,
      2px 2px 8px #000;
    transition: all 0.3s ease;
    z-index: 2;
    position: relative;
  }
  .brand-name:hover {
    text-shadow:
      0 0 20px #ff1744,
      0 0 60px #dc143c,
      0 0 80px #000,
      2px 2px 12px #000;
    letter-spacing: 5px;
  }
  
  @keyframes slideInFromLeft {
    from {
      opacity: 0;
      transform: translateX(-50px);
    }
    to {
      opacity: 1;
      transform: translateX(0);
    }
  }
  
  @keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
  }
  
  .hero-subtitle {
    font-size: 1.8rem;
    color: #ffffff;
    margin-bottom: 2rem;
    text-transform: uppercase;
    letter-spacing: 4px;
    font-weight: 300;
    text-shadow: 
      0 0 10px rgba(220, 20, 60, 0.6),
      2px 2px 8px rgba(0, 0, 0, 0.9);
    animation: fadeInUp 1s ease-out 0.3s both;
  }
  
  .hero-description {
    font-size: 1.2rem;
    line-height: 1.9;
    margin-bottom: 3rem;
    color: #ffffff;
    max-width: 700px;
    margin-left: auto;
    margin-right: auto;
    text-shadow: 
      0 0 8px rgba(220, 20, 60, 0.4),
      2px 2px 6px rgba(0, 0, 0, 0.9);
    animation: fadeInUp 1s ease-out 0.5s both;
  }
  
  .hero-buttons {
    display: flex;
    gap: 1.5rem;
    justify-content: center;
    flex-wrap: wrap;
    animation: fadeInUp 1s ease-out 0.7s both;
  }
  
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
  
  /* Terminal */
  .terminal {
    background-color: #1e1e1e;
    border-radius: 8px;
    box-shadow: 0 0 30px rgba(220, 20, 60, 0.3);
    max-width: 500px;
    font-family: 'Courier New', monospace;
  }
  
  .terminal-header {
    background-color: #2d2d2d;
    padding: 10px 15px;
    border-radius: 8px 8px 0 0;
    display: flex;
    gap: 8px;
  }
  
  .terminal-button {
    width: 12px;
    height: 12px;
    border-radius: 50%;
  }
  
  .terminal-button.red { background-color: #ff5f56; }
  .terminal-button.yellow { background-color: #ffbd2e; }
  .terminal-button.green { background-color: #27ca3f; }
  
  .terminal-body {
    padding: 20px;
    min-height: 200px;
  }
  
  .terminal-line {
    margin-bottom: 10px;
    font-size: 14px;
  }
  
  .prompt {
    color: var(--primary-red);
  }
  
  .command {
    color: #50fa7b;
  }
  
  .output {
    color: var(--text-light);
  }
  
  .typing {
    overflow: hidden;
    border-right: 2px solid var(--primary-red);
    white-space: nowrap;
    animation: typing 3s steps(30, end), blink-caret 0.75s step-end infinite;
  }
  
  @keyframes typing {
    from { width: 0; }
    to { width: 100%; }
  }
  
  @keyframes blink-caret {
    from, to { border-color: transparent; }
    50% { border-color: var(--primary-red); }
  }
  
  /* About Section */
  .about {
    padding: 100px 20px;
    background-color: var(--dark-gray);
    margin: 20px 0;
  }
  
  .section-title {
    font-size: 3rem;
    text-align: center;
    margin-bottom: 4rem;
    background: linear-gradient(90deg, var(--primary-red), #ff4466, var(--primary-red));
    background-size: 200% auto;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    animation: shimmer 3s linear infinite;
    position: relative;
    text-shadow: 0 0 30px rgba(220, 20, 60, 0.5);
  }
  
  .section-title::after {
    content: '';
    position: absolute;
    bottom: -10px;
    left: 50%;
    transform: translateX(-50%);
    width: 0;
    height: 3px;
    background: linear-gradient(90deg, transparent, var(--primary-red), transparent);
    animation: underlineGrow 2s ease-out forwards;
    box-shadow: 0 0 10px var(--primary-red);
  }
  
  @keyframes shimmer {
    to { background-position: 200% center; }
  }
  
  @keyframes underlineGrow {
    to { width: 150px; }
  }
  
  .about-content {
    display: grid;
    grid-template-columns: 45fr 55fr;
    gap: 4rem;
    align-items: start;
  }
  
  .about-left p {
    font-size: 1.1rem;
    margin-bottom: 1.5rem;
    line-height: 1.8;
    color: rgba(255, 255, 255, 0.9);
  }
  
  .about-right h3 {
    font-size: 1.8rem;
    margin-bottom: 2.5rem;
    color: var(--primary-red);
    text-transform: uppercase;
    letter-spacing: 2px;
    text-shadow: 0 0 20px rgba(220, 20, 60, 0.5);
  }
  
  .stats {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.5rem;
    margin-top: 2.5rem;
  }
  
  .stat {
    text-align: center;
    position: relative;
    padding: 1.5rem 1rem;
    background: rgba(220, 20, 60, 0.05);
    border-radius: 8px;
    border: 1px solid rgba(220, 20, 60, 0.2);
    backdrop-filter: blur(10px);
    transition: all 0.3s ease;
  }
  
  .stat:hover {
    transform: translateY(-5px);
    background: rgba(220, 20, 60, 0.1);
    box-shadow: 0 5px 20px rgba(220, 20, 60, 0.3);
    border-color: rgba(220, 20, 60, 0.5);
  }
  
  .stat-number {
    font-size: 2.5rem;
    font-weight: 900;
    color: var(--primary-red);
    text-shadow: 0 0 15px rgba(220, 20, 60, 0.6),
                 0 2px 4px rgba(0, 0, 0, 0.8);
    display: block;
    margin-bottom: 0.5rem;
    line-height: 1;
  }
  
  .stat-label {
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 1.5px;
    color: rgba(255, 255, 255, 0.7);
    font-weight: 500;
  }
  
  @keyframes pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.8; }
  }
  
  /* Accordion Styles */
  .accordion-list {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }
  
  .accordion-item {
    background: rgba(220, 20, 60, 0.03);
    border: 1px solid rgba(220, 20, 60, 0.2);
    border-radius: 8px;
    overflow: hidden;
    transition: all 0.3s ease;
  }
  
  .accordion-item.active {
    border-color: rgba(220, 20, 60, 0.5);
    background: rgba(220, 20, 60, 0.08);
  }
  
  .accordion-header {
    display: flex;
    align-items: center;
    gap: 1rem;
    padding: 1.25rem 1.5rem;
    cursor: pointer;
    transition: all 0.3s ease;
    user-select: none;
  }
  
  .accordion-header:hover {
    background: rgba(220, 20, 60, 0.05);
  }
  
  .accordion-icon {
    font-size: 1.5rem;
    color: var(--primary-red);
    filter: drop-shadow(0 0 8px rgba(220, 20, 60, 0.6));
  }
  
  .accordion-header h4 {
    flex: 1;
    font-size: 1.1rem;
    font-weight: 700;
    color: var(--primary-red);
    text-transform: uppercase;
    letter-spacing: 1px;
    margin: 0;
  }
  
  .accordion-arrow {
    font-size: 1.2rem;
    color: var(--primary-red);
    transition: transform 0.3s ease;
  }
  
  .accordion-item.active .accordion-arrow {
    transform: rotate(180deg);
  }
  
  .accordion-content {
    max-height: 0;
    overflow: hidden;
    transition: max-height 0.4s ease, padding 0.4s ease;
    padding: 0 1.5rem;
  }
  
  .accordion-item.active .accordion-content {
    max-height: 500px;
    padding: 0 1.5rem 1.5rem;
  }
  
  .accordion-content p {
    color: rgba(255, 255, 255, 0.8);
    margin-bottom: 1rem;
    line-height: 1.6;
  }
  
  .skill-details {
    list-style: none;
    padding: 0;
    margin: 0;
  }
  
  .skill-details li {
    padding: 0.5rem 0;
    padding-left: 1.5rem;
    color: rgba(255, 255, 255, 0.7);
    font-size: 0.95rem;
    position: relative;
  }
  
  .skill-details li::before {
    content: '▸';
    position: absolute;
    left: 0;
    color: var(--primary-red);
    font-weight: bold;
  }
  
  /* Services Section */
  .services {
    padding: 100px 20px;
    background-color: var(--black);
    margin: 20px 0;
  }
  
  .services-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
    gap: 2rem;
  }
  
  .service-card {
    background-color: var(--dark-gray);
    padding: 2rem;
    border-radius: 8px;
    border: 1px solid var(--light-gray);
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
  }
  
  .service-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 2px;
    background-color: var(--primary-red);
    transition: left 0.3s ease;
  }
  
  .service-card:hover::before {
    left: 0;
  }
  
  .service-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 10px 30px rgba(220, 20, 60, 0.2);
  }
  
  .service-icon {
    font-size: 3rem;
    margin-bottom: 1rem;
  }

  .service-icon i {
    color: var(--primary-red);
    font-size: 3rem;
  }
  
  .service-card h3 {
    color: var(--primary-red);
    margin-bottom: 1rem;
    font-size: 1.3rem;
  }
  
  .service-card p {
    line-height: 1.6;
  }

  .service-card.coming-soon {
    position: relative;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
  }

  .service-card.coming-soon::after {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(0, 0, 0, 0.7);
    backdrop-filter: blur(4px);
    z-index: 1;
  }

  .coming-soon-badge {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    background: linear-gradient(135deg, var(--primary-red), #ff1744);
    color: white;
    padding: 1.5rem 3.5rem;
    border-radius: 12px;
    font-size: 1.4rem;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 4px;
    z-index: 2;
    box-shadow: 0 10px 40px rgba(220, 20, 60, 0.6),
                0 0 20px rgba(220, 20, 60, 0.4);
    border: 2px solid rgba(255, 255, 255, 0.2);
    animation: pulse-badge 2s ease-in-out infinite;
    text-align: center;
    white-space: nowrap;
  }
  
  @keyframes pulse-badge {
    0%, 100% {
      transform: translate(-50%, -50%) scale(1);
      box-shadow: 0 10px 40px rgba(220, 20, 60, 0.6),
                  0 0 20px rgba(220, 20, 60, 0.4);
    }
    50% {
      transform: translate(-50%, -50%) scale(1.05);
      box-shadow: 0 15px 50px rgba(220, 20, 60, 0.8),
                  0 0 30px rgba(220, 20, 60, 0.6);
    }
  }
    letter-spacing: 2px;
    z-index: 2;
    box-shadow: 0 0 20px rgba(220, 20, 60, 0.5);
    animation: pulse 2s ease-in-out infinite;
  }

  @keyframes pulse {
    0%, 100% {
      transform: translate(-50%, -50%) scale(1);
      box-shadow: 0 0 20px rgba(220, 20, 60, 0.5);
    }
    50% {
      transform: translate(-50%, -50%) scale(1.05);
      box-shadow: 0 0 30px rgba(220, 20, 60, 0.8);
    }
  }
  
  /* Team Section */
  .team {
    padding: 100px 20px;
    background-color: var(--dark-gray);
    margin: 20px 0;
  }
  
  .team-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
    max-width: 1200px;
    margin: 0 auto;
  }
  
  .team-member {
    background-color: var(--black);
    padding: 2rem;
    border-radius: 8px;
    text-align: center;
    border: 1px solid var(--light-gray);
    transition: all 0.3s ease;
  }
  
  .team-member:hover {
    transform: translateY(-5px);
    border-color: var(--primary-red);
  }
  
  .member-avatar {
    font-size: 4rem;
    margin-bottom: 1rem;
  }

  .member-avatar i {
    color: var(--primary-red);
    font-size: 4rem;
  }
  
  .team-member h3 {
    font-size: 0.9rem;
    letter-spacing: 1px;
  }
  
  .member-bio {
    font-size: 0.9rem;
    opacity: 0.8;
  }
  
  .member-email {
    font-size: 0.8rem;
    color: var(--primary-red);
    margin-top: 0.5rem;
    font-weight: bold;
  }
  
  /* Social Media Section */
  .social-media {
    padding: 100px 20px;
    background-color: var(--black);
    margin: 20px 0;
  }
  
  .social-subtitle {
    text-align: center;
    font-size: 1.1rem;
    color: rgba(255, 255, 255, 0.7);
    margin-bottom: 4rem;
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
  }
  
  .social-links {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 2rem;
    max-width: 1400px;
    margin: 0 auto;
  }
  
  .social-card {
    background: rgba(220, 20, 60, 0.03);
    border: 1px solid rgba(220, 20, 60, 0.2);
    border-radius: 12px;
    padding: 3rem 2rem;
    text-align: center;
    text-decoration: none;
    color: inherit;
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
    overflow: hidden;
  }
  
  .social-card::before {
    content: '';
    position: absolute;
    inset: 0;
    background: radial-gradient(circle at center, rgba(220, 20, 60, 0.1), transparent);
    opacity: 0;
    transition: opacity 0.4s ease;
  }
  
  .social-card:hover::before {
    opacity: 1;
  }
  
  .social-card:hover {
    transform: translateY(-10px);
    border-color: rgba(220, 20, 60, 0.6);
    box-shadow: 0 20px 40px rgba(220, 20, 60, 0.3);
    background: rgba(220, 20, 60, 0.08);
  }
  
  .social-icon {
    width: 100px;
    height: 100px;
    margin: 0 auto 1.5rem;
    display: flex;
    align-items: center;
    justify-content: center;
    background: radial-gradient(circle, rgba(220, 20, 60, 0.2), transparent);
    border-radius: 50%;
    border: 2px solid rgba(220, 20, 60, 0.3);
    transition: all 0.4s ease;
  }
  
  .social-card:hover .social-icon {
    transform: scale(1.1) rotate(5deg);
    border-color: rgba(220, 20, 60, 0.6);
    box-shadow: 0 0 30px rgba(220, 20, 60, 0.5);
  }
  
  .social-icon i {
    font-size: 3.5rem;
    color: var(--primary-red);
    filter: drop-shadow(0 0 10px rgba(220, 20, 60, 0.8));
    transition: all 0.4s ease;
  }
  
  .social-card:hover .social-icon i {
    filter: drop-shadow(0 0 20px rgba(220, 20, 60, 1));
  }
  
  .social-card h3 {
    font-size: 1.5rem;
    color: var(--primary-red);
    margin-bottom: 0.5rem;
    text-transform: uppercase;
    letter-spacing: 2px;
    font-weight: 700;
  }
  
  .social-card p {
    font-size: 0.95rem;
    color: rgba(255, 255, 255, 0.7);
    line-height: 1.6;
    transition: color 0.3s ease;
  }
  
  .social-card:hover p {
    color: rgba(255, 255, 255, 0.9);
  }
  
  
  /* Footer */
  .footer {
    background-color: var(--dark-gray);
    padding: 3rem 20px 1rem;
    border-top: 2px solid var(--primary-red);
  }
  
  .footer-content {
    display: grid;
    grid-template-columns: 1fr 2fr;
    gap: 3rem;
    margin-bottom: 2rem;
  }
  
  .footer-brand .logo {
    color: var(--primary-red);
    font-size: 1.5rem;
    margin-bottom: 0.5rem;
  }
  
  .footer-tagline {
    font-size: 0.9rem;
    color: rgba(255, 255, 255, 0.6);
    margin-top: 0.5rem;
    font-style: italic;
  }
  
  .footer-links {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
  }
  
  .footer-column h4 {
    color: var(--primary-red);
    margin-bottom: 1rem;
  }
  
  .footer-column ul {
    list-style: none;
  }
  
  .footer-column li {
    margin-bottom: 0.5rem;
  }
  
  .footer-column a {
    color: var(--text-light);
    text-decoration: none;
    transition: color 0.3s ease;
    font-size: 0.95rem;
  }
  
  .footer-column a:hover {
    color: var(--primary-red);
  }
  
  .footer-bottom {
    padding-top: 2rem;
    border-top: 1px solid var(--light-gray);
    text-align: center;
  }
  
  .footer-bottom p {
    margin: 0.5rem 0;
  }
  
  .footer-subtitle {
    font-size: 0.85rem;
    color: rgba(255, 255, 255, 0.5);
    margin-top: 0.5rem;
  }
  
  .footer-warning {
    color: var(--primary-red);
    font-weight: bold;
  }

  .footer-warning i {
    color: var(--primary-red);
    font-size: 1rem;
    vertical-align: middle;
  }
  
  /* Responsive Design */
  @media (max-width: 768px) {
    .hero {
      min-height: auto;
      height: auto;
      padding: 80px 20px 40px;
    }

    .hero .container {
      grid-template-columns: 1fr;
      gap: 2.5rem;
      padding: 1rem;
      min-height: auto;
      align-items: start;
    }
    
    .hero-content {
      order: 1;
    }
    
    .hero-stats {
      order: 2;
      width: 100%;
      margin-bottom: 2rem;
      padding: 0;
      max-width: 100%;
    }
    
    .stats-card {
      padding: 1.5rem;
      width: 100%;
      max-width: 92%;
      margin: 0 auto;
    }
    
    .best-badge {
      font-size: 1rem;
      padding: 1rem 0.8rem;
      gap: 0.5rem;
    }
    
    .best-badge i {
      font-size: 1.5rem;
    }
    
    .best-badge .flag {
      font-size: 1.3rem;
    }
    
    .stat-item {
      padding: 0.8rem;
    }

    .stat-item i {
      font-size: 1.5rem;
    }
    
    .stat-value {
      font-size: 1.3rem;
    }

    .stat-label {
      font-size: 0.85rem;
    }
    
    .brand-identity {
      flex-direction: column;
      gap: 1rem;
      margin-bottom: 1.5rem;
    }
    
    .brand-logo {
      width: 100px;
      height: 100px;
    }
    
    .brand-name {
      font-size: 2.5rem;
      letter-spacing: 2px;
    }
    
    .hero-subtitle {
      font-size: 1.2rem;
      letter-spacing: 2px;
      margin-bottom: 1.5rem;
    }
    
    .hero-description {
      font-size: 0.95rem;
      line-height: 1.6;
      margin-bottom: 2rem;
      padding: 0;
    }
    
    .hero-buttons {
      flex-direction: column;
      padding: 0;
      gap: 1rem;
    }
    
    .hero-buttons .btn {
      width: 100%;
    }
    
    .about-content {
      grid-template-columns: 1fr;
      gap: 3rem;
    }
    
    .stats {
      grid-template-columns: 1fr;
      max-width: 300px;
      margin: 2.5rem auto 0;
    }
    
    .accordion-list {
      gap: 0.75rem;
    }
    
    .accordion-header {
      padding: 1rem;
    }
    
    .accordion-header h4 {
      font-size: 0.95rem;
    }
    
    .accordion-icon {
      font-size: 1.2rem;
    }
    
    .stats {
      justify-content: center;
    }
    
    .services-grid {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }

    .team-grid {
      grid-template-columns: 1fr;
    }
    
    .social-links {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }
    
    .social-card {
      padding: 2rem 1.5rem;
    }
    
    .social-icon {
      width: 80px;
      height: 80px;
    }
    
    .social-icon i {
      font-size: 2.5rem;
    }
    
    .footer-content {
      grid-template-columns: 1fr;
    }
    
    .footer-links {
      grid-template-columns: 1fr;
    }
    
    .section-title {
      font-size: 2rem;
    }
    
    /* Reduce padding on mobile for better space utilization */
    .hero,
    .about,
    .services,
    .team,
    .social-media,
    .footer {
      padding-left: 10px;
      padding-right: 10px;
    }
  }

  /* Extra small devices (phones, less than 480px) */
  @media (max-width: 480px) {
    .hero {
      padding: 70px 15px 30px;
    }

    .hero .container {
      padding: 1rem;
    }

    .stats-card {
      padding: 1.2rem;
      max-width: 92%;
    }

    .brand-name {
      font-size: 2rem;
      letter-spacing: 1px;
    }

    .brand-logo {
      width: 80px;
      height: 80px;
    }

    .hero-subtitle {
      font-size: 1rem;
      letter-spacing: 1px;
    }

    .hero-description {
      font-size: 0.9rem;
    }

    .best-badge {
      font-size: 0.9rem;
      padding: 0.8rem 0.6rem;
    }
    
    .best-badge i {
      font-size: 1.2rem;
    }
    
    .best-badge .flag {
      font-size: 1.1rem;
    }
    
    .stats-card {
      padding: 1.2rem;
    }
    
    .stat-value {
      font-size: 1.2rem;
    }
    
    .stat-label {
      font-size: 0.8rem;
    }

    .stat-item i {
      font-size: 1.3rem;
    }
  }

  /* Very small devices (phones, less than 375px) */
  @media (max-width: 375px) {
    .hero {
      padding: 70px 15px 30px;
    }

    .hero .container {
      padding: 1rem;
    }

    .stats-card {
      padding: 1rem;
      max-width: 92%;
    }
    .brand-name {
      font-size: 1.8rem;
    }

    .brand-logo {
      width: 70px;
      height: 70px;
    }

    .hero-subtitle {
      font-size: 0.9rem;
    }

    .hero-description {
      font-size: 0.85rem;
    }

    .best-badge {
      font-size: 0.8rem;
      padding: 0.7rem 0.5rem;
      gap: 0.4rem;
    }
    
    .best-badge i {
      font-size: 1.1rem;
    }
    
    .best-badge .flag {
      font-size: 1rem;
    }
    
    .stats-card {
      padding: 1rem;
    }

    .stat-value {
      font-size: 1.1rem;
    }

    .stat-item i {
      font-size: 1.2rem;
    }
  }
</style>